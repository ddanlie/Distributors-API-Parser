### Run tests (not ready):
from multi-b2b-api folder:  **.vent/bin/python -m parser_tests.clientcore-tests**

### Queries
To be rewritten as full endpoints copy with dynamic param args
Currently POSTMAN is used